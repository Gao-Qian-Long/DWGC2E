# 后续人工验收清单

2026-09-16：用户决定测试验收后续自行进行。本清单只是交接步骤，未勾选项不表示已通过；不要求现在登录、重启CAD或提供密码。始终使用原图的副本，输出到新目录。不得使用真实支付作为测试。

## 验收记录

记录程序版本、日期、CAD版本、测试图纸副本名、期望与实际结果。截图和日志请先遮挡邮箱、令牌及业务内容，再放到本任务artifacts目录；不要放源码根目录。

- [ ] 启动 D:/DWGC2E/release/DwgTranslator.exe，页面切换正常，无资源缺失。
- [ ] 自行登录；账户、会员、额度、设备信息能加载。不得将密码/令牌写进报告。
- [ ] 添加测试DWG/DXF副本；实体与文件数量正确，原文件未改变。
- [ ] 默认术语库可找到、加载；个人词库可读写，重启后保留。
- [ ] CAD检测正确；插件平台与宿主匹配，插件路径指向当前安装。
- [ ] 从APP创建小规模翻译任务；观察真实云端返回、任务状态及错误提示。
- [ ] 从APP导出到新目录；CAD打开结果，核对文字、输出数量和原图不变。回写算法本轮不修改。
- [ ] 设置保存、重启读取正常；日志写到正式数据目录，源码目录不新增用户运行文件。
- [ ] 会员购买窗口可打开并关闭；不提交真实订单或付款。
- [ ] 检查更新能返回合理结果；真正替换安装版本仅在备份完成且无任务运行时单独验收。

## 安装专项（以后单独进行）

- [ ] 使用隔离目录（不得用源码根D:/DWGC2E）验证首次安装、升级、卸载与数据保留。
- [x] 已用隔离测试进程验证便携安装/卸载拒绝同目标运行实例，且不终止进程；已验证安装目标根、安装包根及卸载脚本目录的联接拒绝。18项检查通过；不涵盖运行中路径替换竞态。
- [x] 快捷方式自动回归：创建函数10项、实际StartMenu卸载30项、旧清单及-NoShortcuts兼容已验证；真实Desktop已知目录卸载8场景27项通过并恢复原状。资源管理器点击启动、其他重定向桌面环境未作为本项证明。
- [ ] 不同语义版本的数据迁移；已有两次2.1.1构建间升级证据不替代此项。
- [ ] 安装途中故障恢复。便携安装目前不是事务式升级，完整自动回滚仍为未实施事项，不能用验收勾选来代替开发。

## 已有证据的范围

本机浩辰2024自动加载已确认。恢复测试后的证据：便携安装/卸载65项、创建快捷方式10项、StartMenu端到端卸载30项、运行实例及根目录联接18项；本次Inno同版本安装/修复/卸载及目标保护45项。此前Inno46项含历史跨构建测试，与本次范围不同，不合并冒充当前完整通过。以上均不替代真实业务端到端验收。详细证据见 artifacts/installer-safety-20260916-resumed/report.md 及 governance-current-status.md。
- [ ] 安装目标保护完整验收：便携 .sln/.git 拒绝及无文件变化已验证，D/C默认选择函数已验证；盘根运行验证及Inno全部拒绝边界尚未完成。

> 2026-09-16 最新执行口径：按用户明确要求，本任务跳过全部人工验收，交由用户后续自行测试；这些项目不再阻塞工程整理收尾，也不计为已通过。清单保留为后续测试交接，不要求用户现在登录、操作CAD或测试支付。自动化回归、UI烟测、构建/发布安全门禁仍必须执行。未实现的开发项（例如安装事务回滚）不属于人工验收豁免，继续单列。

- [x] 中文安装入口自动验证13项通过：生产打包编码、中文及空格目录、安装参数、完整中文提示、无命令解析错误、失败退出码及目标保留。实际资源管理器双击和屏幕显示仍留待人工验收。

## 校对保存与恢复验收（已更新实现状态）
192900及后续交付已实现账号隔离校对快照，201231保留此功能；不再只是修改内存。Core、受控UI与独立进程恢复已有自动证据，详见proofreading-persistence.md。保存校对不等于图纸已经导出，也不承诺未点击保存的内容自动恢复。

- [ ] 明确点击保存校对，正常关闭并重启APP，同账号同图纸恢复人工译文。
- [ ] 切换账号不显示另一账号草稿；切回原账号正确恢复。
- [ ] 原图变化、保存失败或快照损坏时明确提示，不错套旧图纸译文。
以上人工场景尚未勾选，不用自动测试结果代替用户验收。

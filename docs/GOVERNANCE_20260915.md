# 2026-09-15 后续执行状态（优先于下方历史记录）

用户后来明确授权全部测试数据可重置。本批采用新空 D1 切换，旧库保留作恢复档案，不再等待历史账号分类。Worker 已部署版本 318abce7-a28d-480f-9692-51c7f7ca8fe2；网页直接发布 6a49cad1.dwgc2e-website.pages.dev，后续 Git 自动发布以实际部署记录为准。

- 后端测试 130/130、类型检查通过；新库 42 个标准结构对象、8 条迁移记录、切换前 0 用户核验通过。
- 网站 14/14 请求/代理测试、15 页面 × 5 宽度 = 75 次布局/脚本检查通过；模拟场景不冒充真实支付。
- APP 核心 93/93、受控 UI smoke 通过；详细边界见 history/desktop-20260916/APP_GOVERNANCE_20260915.md。
- 线上 health/plans 为 200；未登录 profile/devices 为 401；内部 tests/docs 不再作为公开静态资源发布。
- Pages Git 构建改成白名单公开目录，避免自动部署上传内部文档及测试脚本。
- 本轮尝试清理三个旧 release-backup 目录，但执行工具在运行前拒绝递归删除操作；没有绕过限制，也没有声称这些目录已删除。%SystemDrive% 等未查清生成原因的目录继续保留。
- 不宣称整份六阶段方案全部完成：全量安全审计、完整 APP/CAD 拆分、实机 CAD 回归、远程恢复演练、公开新版 APP 下载与目录清理仍有未完成项。

数据库执行/回滚记录见 cf-worker/ops/RELEASE_20260915.md。保护基线和敏感日志仍在仓库外，不推送 Git。

---

## 以下为前一批历史审查记录（其中“尚未部署/等待历史分类”已被上方新授权与执行状态取代）

# 2026-09-15 治理实施记录（第一批，本地未发布）

## 状态与范围

本记录不是全计划完成声明。阶段 0 已建立可恢复文件基线；阶段 1 已实现新的本地设备/会话模型并完成自动测试，但历史迁移和真实已发行客户端验证未完成，**禁止将此批 Worker 或依赖它的网站直接发布生产**。阶段 2–5 仅实施下面明确列出的改动。

## 基线与恢复

- 受限访问仓库外恢复目录：`D:\DWGC2E_Baselines\20260915-102235`，包括 Git、忽略文件、配置、未跟踪文件及文件 SHA256 清单。敏感配置不得复制到问题台账、普通日志或 Git。
- APP/Worker 初始提交：`553eef2b30415699d1dd8d9cdef0e86b30e2ce88`。
- 网站初始提交：`fb8c0919da58e82a05fd5f884115c18a9377a7ec`。
- 不使用 reset/clean 恢复工作区。需要恢复时先复制到单独目录并按清单核对，再选择性恢复；不得覆盖后续有效工作。
- 生产仅查询部署/结构元数据与必要聚合；本批没有生产写入，没有真实邮件、付款或测试账号。
- 当前线上网站为此前直接部署的工作区快照；本地基线已收录，但 Git 发布源尚未推送对齐，自动部署仍有覆盖风险。

## 本地交付

### Worker

- 网页独立登录 `/v1/auth/web/login`；原 APP `/v1/auth/login` 保留成功响应契约。
- 新表：账号范围 APP 安装绑定、会话用途、迁移审计；三实例上限由数据库触发器约束。
- 认证区分会话和绑定；网页不能翻译执行或任意新增绑定；解绑只影响本人该安装实例及相关会话。
- 服务端退出只撤销当前会话，不解除安装绑定。
- 资料保存真实落库；禁止直接更换验证邮箱；改密校验旧密码并撤销全部该账号会话。
- 登录签发批次再次校验密码版本/账号状态，防止密码变更后并发旧登录继续签发。
- 支付管理查询复用受限响应读取并核对商户、订单、渠道、金额，维持只读，不据此结算。
- 固定现有 lock 中的开发依赖版本，未升级依赖。

### 网站

- 删除网页登录自动生成 APP 设备编号的逻辑，切换网页登录入口。
- 请求层区分认证过期、凭证错误、取消、超时和其他错误；账号变化后丢弃旧响应；替代 Authorization 的错误不能清除当前账号。
- 服务端退出；表单重复提交/登录模式切换保护；设备页解释 APP 安装限额与解绑效果。
- 资料页移除虚假接口文案，邮箱只读；改密补齐当前密码字段，成功后清除已失效本地登录。
- 保留当前奶白/墨黑/琥珀设计，不改公开路径。

### APP

- 设备身份独立服务：文件落盘、并发初始化锁；损坏/存储失败不再随机创建临时安装编号。
- 抽出 WorkerApiClient 请求传输与会话部分，保留原调用门面。
- 登录不带旧 bearer；迟到的旧会话响应取消；退出先确认服务端撤销。
- 正在执行任务或刷新账户时不允许换号，账户切换中受 RequireAccount 保护的操作不能启动。
- **尚未完成本地任务、术语和缓存按账号隔离，不得宣称全面防串号。**

## 测试记录

日志目录同基线目录；测试均为本地隔离数据。

| 项目 | 结果与边界 |
|---|---|
| Worker | 126/126；含网页零占用、并发名额、跨账号撤销、权限负向、改密竞态、迁移保留与事务回滚 |
| Worker 类型检查 | 通过 |
| 网站请求层/表单契约 | 10/10；不代表全部页面浏览器验收 |
| APP Core net8 Release | 90/90（基线 82/82） |
| APP UI 冒烟 | UI_SMOKE=PASS；受控 API，不是物理 DPI 或真实 CAD 验证 |
| 生产结构离线预演 | 从生产元数据重建 SQLite，应用 0006/0007，四个新增表列定义与新库一致；不是全库结构一致结论 |
| 未验证 | 五档宽度逐页、生产付款/邮件、真实发行 APP、AutoCAD、完整干净打包、生产恢复演练 |

原 Worker 基线存在导入加载器失败；修复测试加载方式后暴露陈旧支付夹具，已同步实际协议并保留身份/金额校验要求。未把这些基线失败记为本轮业务回归。

## 问题台账与发布门槛

| 编号/级别 | 证据与影响 | 状态 / 下一步 |
|---|---|---|
| AUTH-01 P1 | 原认证依赖全局 devices，网页占 APP 槽位且同机换号冲突 | 本地修复，待审阅迁移及发布 |
| AUTH-02 P1 | 临时安装编号会重复占用；传输返回未防旧账号迟到 | 本地修复并测试；本地业务数据隔离另列 APP-01 |
| AUTH-03 P1 | 全部旧会话没有 session_contexts，新认证将拒绝这些会话 | 发布阻断：必须先出历史映射与影响清单 |
| DATA-01 P1 | 迁移账仅 0001/0002，实际已有支付结构 | 未解决：逐对象前置检查，禁止全量重跑或仅补账 |
| DATA-02 P1 | 生产未发现 feedback 表，本地路由已有 | 0006 离线预演通过，未上生产；需核对独立反馈发布 |
| DATA-03 P1 | 历史分类：web 候选活跃 2/撤销 1，APP 候选活跃 1，未知活跃 2 | 聚合证据不是最终审批；未知不自动删除/授权 |
| APP-01 P1 | 任务/术语/缓存仍有全局本地状态 | 未完成：先确定旧数据归属/隔离迁移，再做换号专项测试 |
| WEB-01 P2 | 页面接口文案/调用与服务端不完全一致 | 资料/设备已修；术语/历史/支付/健康/反馈全链路审查待完成 |
| OPS-01 P1 | 直接发布网站与 Git 源未对齐 | 基线入本地 Git；推送前核对 Pages 自动部署，不能先推网站 |
| OPS-02 P2 | 恢复能力未在独立远程环境演练 | 本地恢复副本已校验；远程恢复演练未完成 |
| CLEAN-01 P2 | 测试相对输出路径留下空目录 | 改为隔离临时目录；清理见下面记录 |
| CLEAN-02 P2 | `%SystemDrive%` 路径生成原因未完全追溯 | 保留，不盲删；不视作生产数据清理授权 |

## 历史迁移/发布操作要求（尚非可执行迁移包）

1. 发布前重读 Worker/Pages 版本、绑定、D1 表/索引/触发器与账本，不复用旧快照作授权依据。
2. 在受保护位置形成最小逐条清单：账号与安装关系、旧会话映射、分类、交叉证据、审批人与时间；普通日志只留数量及操作编号。
3. web 前缀不单独构成充分证据。确认 APP 才建立绑定并映射有效 APP 会话；确认网页撤销旧会话但保留旧表和审计；未知不映射，不授予 APP 权限，明确重登录影响。
4. 迁移需断言源状态未变、每账号不超过三绑定；在隔离库覆盖重复运行、部分失败、回滚与重试。
5. 准备理解新结构的兼容回退 Worker。不得只回滚到依赖旧 devices 的版本，也不得整库恢复覆盖新订单。
6. 明确迁移期间旧 Worker 写入的竞态处理（受控维护窗口或兼容双写阶段）；当前批次未实现双写，不能让旧登录在扫描后悄悄生成漏迁移会话。
7. 分类影响清单、迁移脚本、回退版本、发行 APP 契约演练都通过后，才按后端兼容扩展→后端→网站→APP 分批发布并记录实际版本。
8. 未达到以上门槛前，不推送触发网站自动发布，不执行远程 migrations apply。

## 安全清理记录

| 绝对路径 | 证据 / 恢复来源 | 处理 |
|---|---|---|
| `D:\DWGC2E\must-survive-write-failure` | UI smoke 旧相对 ExportDirectory 生成；目录为空；外部基线保留同名目录；源已改绝对临时路径 | 校验完整路径、空目录和恢复来源后非递归删除 |
| `D:\DWGC2E\%SystemDrive%` | 原因未完全复核 | 未删除 |
| 两仓库根 `.wrangler` | 本地状态不是发布资产 | 仅添加忽略规则，未删除 |
| release/artifacts/图纸/配置/业务数据 | 未完成逐项引用证明 | 未删除 |

后续会话、验证码、限流与缓存生命周期只做统计/预演；生产自动删除保持关闭，订单与结算不属于垃圾清理范围。

## 下一批顺序

1. 完成历史分类映射、账本差异解释、兼容回退和生产升级演练，解除 AUTH-03/DATA-01/OPS-01 门槛。
2. 完成网站全链路及五档浏览器验证，再独立发布设备与会话修复。
3. APP 按账号数据隔离与异常恢复专项；然后才拆任务、CAD 写回与界面职责。
4. 逐项引用证明后继续清理，最后以干净副本构建/测试/打包验收。

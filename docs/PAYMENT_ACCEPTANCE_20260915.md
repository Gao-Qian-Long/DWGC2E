# 支付修复验收与发布记录

更新时间：2026-09-15（北京时间）。本轮未创建真实订单、未扫码付款、未自动退款，也未删除历史交易。

## 交付状态

| 项目 | 结果 |
|---|---|
| Worker 兼容修复 | 修复部署 fde657a0-6def-4da2-bb71-8b9867e81ba4；16:16 核验当前线上 e3b52688-0655-48fc-b4a7-dabc3b41d33b（管理员密钥外部更新，脚本 etag 相同） |
| 0008 增量迁移 | 已应用，迁移前备份完成；生产结构隔离预检通过 |
| 网页修复 | 已发布 Pages fef7c0ea-0c87-4e23-be8a-23116610d199，2026-09-15 15:11 |
| 网页源代码 | main 提交 0d766b0f776108b16a9b6d05667ae4a36a485f8a；包含之前的 45c86dc 网页交互修复 |
| APP | 已生成本地候选包；未替换公开下载、未覆盖当前 release |
| 生产新下单 | 关闭；未授权验收账号、未全量放行 |
| 定时恢复线上运行 | **尚未验收通过，详见阻断项 B1** |

## 自动化与交互证据

| 层次 | 结果与边界 |
|---|---|
| Worker | 146/146，通过类型检查；覆盖早到／重复／冲突回调、事务回滚、未知创建、隐藏／过期后付款、幂等、权益快照、越权、限流、人工审计及恢复租约 |
| 真实本地 D1 | workerd/Miniflare D1 测试通过；并发不同交易只允许一笔、重复回调不重复续期、注入事务失败回滚绑定及权益，移除故障后可重试。追加真实 workerd scheduled 入口测试：到期任务转人工、释放租约、重复触发不重复处理、不产生结算；另验证调度失败不被吞掉且日志脱敏。不是生产故障注入 |
| Core | 120/120；包含 17 项独立支付客户端用例（认证、幂等、换号迟到响应、确认请求、快照解析、不完整响应、异常数值、错单与购买内容不符、失败重试保留原键） |
| 原生 APP 窗口 | Windows 实际启动窗口、模拟服务端；验证原意图恢复、关闭重开不新建单、二维码显示／到期、成功后同步失败、重试成功、换号关闭。截图人工目视检查通过。未进行手机实际扫码、真实账户支付或硬件 DPI 验收 |
| APP 打包 | Release、自包含 Windows x64；依赖与 CAD 插件包结构检查通过。不是 AutoCAD/GstarCAD 实际图纸写回验收 |
| 网页请求／代理 | 14/14 |
| 网页布局 | 15 页 × 5 宽度=75 次检查，无横向溢出／脚本错误 |
| 网页支付交互 | 390/1440 两种宽度通过：二维码、重载恢复、跨标签同意图、独立到期隐藏、到账与同步失败区分、重试恢复、会话切换保护。从干净 Git 归档再次运行通过 |
| 其他网页交互防回退 | 新的 22/22 回归在合并支付改动后通过；保留另一任务的账户／术语修复 |

自动测试与界面脚本全部使用隔离模拟数据，不支付、不发邮件。截图中的二维码为不可付款测试内容；不将图片生成或语法检查当作真实支付验收。

主要本地证据：
- artifacts/payment-worker-tests-continuation.log（本次 146 项）
- artifacts/payment-cron-platform-evidence.txt（仅本地诊断，外发需复核）
- artifacts/payment-worker-tests.log
- artifacts/payment-release-continuation.log（当前 APP 候选包与 120 项 Core 测试、Windows UI smoke）
- artifacts/payment-release-final.log（早期候选包）
- artifacts/payment-combined-ui-regressions.log
- artifacts/payment-web-full-smoke.log
- artifacts/ui-smoke/billing-native-qr.png、billing-native-paid.png
- artifacts/payment-browser/billing-paid-390.png、billing-paid-1440.png
- artifacts/payment-release-verification.json、payment-site-verification.json

## 生产非破坏性复核

- 迁移前后订单 2 笔、结算 1 笔、会员订阅 1 笔，计数未变；paid 但缺失结算流水=0。
- 1 笔历史 pending/ready 订单已回填恢复任务；未批量修改其付款状态。该订单仍需要人工归属、证据核实及处理结论。
- 绑定配置逐项比较保持一致；没有替换密钥、商户设置、邮件设置或下载配置。
- 匿名 plans=200 且 paymentsEnabled=false；订单、权益快照和管理接口未认证=401；缺少参数的回调=400。
- 网站正式域名的 billing.js、api-client.js、account.js、terminology.js、billing.html 的 SHA256 与对应 Git 原始文件一致。使用 Git 原始 LF 文件核验，避免 Windows 归档的 CRLF 差异被误判为发布错误。
- 网站未公开 tests 或 tools；相关 URL=404；权益 API 未登录=401。

## APP 候选包

- 版本：2.1.1+ui.20260915-161545.a3f6f1f。
- 路径：D:/DWGC2E/artifacts/DwgTranslator-win-x64-GstarCAD-20260915-161545.zip。
- 可执行文件：D:/DWGC2E/artifacts/publish-20260915-161545/DwgTranslator.exe。
- EXE SHA256：76C6F335ADF436E9F9AB5FE001DDA4C1B0F2154A6037099719C7506B692B44F9。
- 后缀 revision 是构建时基线提交；本轮 APP/Worker 修改仍保留在工作区，不能把基线提交单独当作候选包全部源码。构建时间和 EXE 校验值才是本候选包准确标识。
- 早期构建候选包仅用于迭代，以上最终路径为本次交付；公开下载仍是此前发行版，不声称已同步正式 APP 发行。

## 未通过／待验收清单（禁止全量开放）

### B1：线上每分钟调度实际执行未证实

截至 2026-09-15 16:16 复核：Cloudflare schedules 为每分钟；当前版本 handlers 包含 scheduled、fetch；D1 服务器时间正常，确有 1 个到期任务，但该任务 attempts=0、state=queued。观察日志只取得 HTTP 事件，未取得定时执行事件。**不是“恢复任务已验证可正常运行”。** 尚不能确定是平台派发还是运行环境问题，不能声称已经定位根因。

本次继续复核：队列 attempts 仍为 0，未创建订单或支付。管理员密钥外部更新后的版本与本轮修复脚本 etag 相同，保留 scheduled/fetch，不覆盖用户密钥变更。

本次补充：平台 GraphQL workersInvocationsScheduled 查询 07:00 UTC 后返回空数组；HTTP 调用记录可正常读取，但这不能单独证明平台故障。16:04 新版本增加 started/completed/failed 日志，仅包含随机运行 ID、调度时间与耗时，不记录订单、账号、商户或数据库错误。新版本短时观察仍无调度记录，不能排除传播延迟；旧版本超过一小时未执行的事实仍未解决。未手动运行生产恢复、未改生产订单状态。

后续验收必须取得至少一次真实 scheduled 执行证据，以及同一任务的 attempts / next_attempt_at / lease 状态正常变化；并验证并发调度不越过退避。未通过时继续停购；既有可信回调可以独立结算。

### B2：可信主动查单合同未验证

当前未认证 findorder 只能诊断，恢复任务即使被调度也不会根据该结果补发权益。需支付平台提供并验证商户认证查询协议；否则“回调丢失自动补单”不得验收，只能使用可信回调重放及人工核实队列。

### B3：受控真实支付与跨端验收未执行

| 场景 | 结果 |
|---|---|
| 网页 × 微信 | 未执行，等待单独授权 |
| 网页 × 支付宝 | 未执行，等待单独授权 |
| APP × 微信 | 未执行，等待单独授权 |
| APP × 支付宝 | 未执行，等待单独授权 |
| 有效期内真实续购 | 未执行 |
| 服务端确认后 10 秒内当前购买页到账 | 本地轮询逻辑已实现，真实链路未测量 |
| 24 小时受控观察 | 未完成 |
| 新版本公开下载同步 | 等全部关键验收通过后执行 |

每笔真实验收须记录：脱敏账号、平台交易、订单、唯一结算、付款确认时间、客户端展示时间、付款前后会员和额度、续购到期变化。不得只记录“页面显示成功”。

## 总结

可交付的是：代码修复、迁移、停购状态的服务端与网页发布、Windows 候选包、测试及运维手册。**不能交付“全部计划已验收完成”或“整个商业支付体系已经成熟”的结论。** 本期以回调与事务的一致性为基础，继续受控验收；退款和完整财务对账另立下一期。

## APP 构建产物保留策略（2026-09-15）

用户要求清理过时版本。本次删除 34 个旧 APP 构建目录、重复压缩包及过时备份，释放约 2.33 GiB；历史日志记录保留，但其中早期候选包路径不再有效。

保留当前 release、每个 CAD 平台最新验证候选目录与 ZIP、当前正式版 ZIP、一份最近回滚备份。成功打包后自动执行 tools/Clean-DesktopArtifacts.ps1；构建失败不触发清理。独有便携设置/词库/导出/日志、正在运行的 APP、数据库备份及非 APP 产物不自动删除。清理工具支持预览，拒绝链接路径；隔离夹具测试通过。

## 最新本地安装状态（2026-09-15 16:31，优先于上文候选包记录）

按用户明确要求，正常 APP 构建每次必须更新 D:/DWGC2E/release，不再仅交付候选目录。

- 已完成完整测试、构建与本地替换：2.1.1+ui.20260915-163050.a3f6f1f。
- 日常启动：D:/DWGC2E/release/DwgTranslator.exe。
- 当前 ZIP：D:/DWGC2E/artifacts/DwgTranslator-win-x64-GstarCAD-20260915-163050.zip。
- 安装后 EXE SHA256：80BE1D5369305554EF031E48E5117AAA1B3730C36725FC5066D9713246B2D876，已与构建记录匹配。
- Core 120/120，Windows UI_SMOKE=PASS；原有 3 个便携设置/词库/提示文件逐项校验保留。
- 旧候选 161545 已由自动清理删除；只留最近回滚目录 release-backup-20260915-163050。
- 仅更新本地安装；网站公开下载未更改，支付停购和真实验收门禁不变。

## 登录修复与最新本地交付（2026-09-15 17:23，覆盖前述本地版本记录）

- 已安装版本：`2.1.1+ui.20260915-172219.a3f6f1f`。
- 启动文件：`D:/DWGC2E/release/DwgTranslator.exe`。
- 安装后 SHA256：`E131067F6522E4594D0AF22FD22BA3EE5386686A3FA72A9C2B0888F915E63E6F`，与构建记录一致。
- 分发候选：`D:/DWGC2E/artifacts/DwgTranslator-win-x64-GstarCAD-20260915-172219.zip`；仅保留最近回滚备份 `D:/DWGC2E/artifacts/release-backup-20260915-172219`（自动清理可能保留包含独有用户数据的目录）。
- 修复：未验证的已保存会话不再禁止密码登录；会员快照非认证错误不再阻断账户验证；登录失败保留密码，成功或离开页面清空；提交时刷新账号绑定并防重复点击。会员接口认证错误仍使会话过期，不放宽身份校验。
- Core 120/120；可见 Windows 账户页 UI 回归通过，包括旧会话登录、失败保留密码、成功清空、双击去重、会员同步失败与认证失效。测试等待异步登录真正结束，而非仅等待界面空闲。支付二维码、停购提示、到期提示、账户切换等回归通过。日志：`D:/DWGC2E/artifacts/login-recovery-fix.log`。
- 清理夹具测试通过；settings.json、默认词库及提示文件与替换前备份哈希逐项相同。未停止用户进程。
- 只读服务健康检查 HTTP 200；未使用用户密码或提交真实登录，因此不能声称用户真实账号已验收登录成功。
- 本版包含此前二维码展示修复：停购原因持续展示、原有有效订单仍能展示二维码、过期原因明确。未恢复新下单、未进行真实支付、未更新网站公开下载。
- 仍需完成生产定时恢复执行核实、可信平台查单能力核实、授权后的四条支付链路/续购及 24 小时受控观察。本次交付不代表整体支付计划完成。

## 用户明确授权全量开放（2026-09-15，开关状态更新）

- 用户明确要求“开放所有购买，网页直接上线”。据此将线上 PAYMENTS_ENABLED 从 false 改为 true；上述停购决定已由本次明确授权覆盖，未完成验收的事实不变。
- 仅通过 Cloudflare 设置接口修改购买绑定。核验其他绑定保持一致，线上 Worker 代码内容哈希不变；没有部署本地 Worker 工作树或覆盖密钥。
- 同步本地 cf-worker/wrangler.toml 的购买开关，防止后续部署意外恢复停购。
- 正式网站 /api/v1/billing/plans 返回 200、paymentsEnabled=true；健康接口 API/数据库正常。所有普通登录用户（含新注册用户）不再被停购开关拦截，原登录与订单校验仍保留。
- 当前线上仅有两个已启用套餐：test_pro_019 / Pro 7 天 A / 0.19 元；test_pro_029 / Pro 7 天 B / 0.29 元。未新增套餐、修改价格或更改权益，已向用户明确说明目前是测试价套餐。
- 本次未创建真实订单、付款、发邮件或改动用户权益；真实支付与自动补单的待验收项仍未完成，不能宣称完整支付链路验收通过。
- 证据目录：D:/DWGC2E/artifacts/purchase-open-20260915。

## 2026-09-15 二维码创建与网页旧订单修复

已发布 Worker fb4e524b-d7c9-4802-8911-df3e1b865cbf（100%）及网站 b7f2ae5。新建二维码不再依赖未认证 findorder 二次查询；只兼容实际观察到的 code=1、交易号和二维码成功响应，并拒绝已返回字段的矛盾。未返回金额时采用签名请求中的服务器定价，不宣称独立查单确认金额；到账仍要求可信签名回调和原有结算检查。

删除记录关闭面板和计时；无二维码不计时；未确认旧单再次购买需明确确认新建且不取消旧单。既有 unknown 订单不自动重建。154 项后端测试、类型检查、网页回归和75项宽度检查通过，正式资源和后端代码已核验。未真实付款，不代表支付全链路/恢复调度验收完成。详情 artifacts/qr-investigation-20260915/RELEASE.md。

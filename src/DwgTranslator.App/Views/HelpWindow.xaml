<Window x:Class="DwgTranslator.App.Views.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:DwgTranslator.App.Views"
        Title="QLCAD 帮助中心"
        Width="900" Height="720" MinWidth="620" MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1" Padding="24,18">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="帮助中心" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource Brush.TextPrimary}"/>
                    <TextBlock Text="使用说明、常见操作与问题排查" Style="{StaticResource CaptionStyle}" Margin="{DynamicResource Spacing.BlockTopTight}"/>
                </StackPanel>
                <Button Grid.Column="1" Content="关闭" Click="Close_Click" Style="{StaticResource Button.Secondary}" VerticalAlignment="Center"/>
            </Grid>
        </Border>
        <views:HelpPanel Grid.Row="1" />
    </Grid>
</Window>
